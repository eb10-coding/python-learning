# Python Learning Log

Learning Python from scratch as an engineering student, building small projects day by day. No AI-assisted autocomplete used while writing these — the goal is understanding, not just working code.

## Progress

- **Day 1 — Lists & dictionaries:** score averages, pass/fail logic, collecting user input into a list of dictionaries
- **Day 2 — Functions:** basic function with a default parameter (discount calculation)
- **Day 3 — Functions & files:** temperature logger, tax calculation, default/keyword arguments, `os`/file writing, persistent text-file sales log
- **Day 4 — Working with files & JSON:** grade tracker (text file), first JSON-backed inventory manager
- **Day 5 — JSON projects:** movie watchlist, quest log (multiple modes: view/add/complete)
- **Day 6 — JSON projects continued:** student grade tracker with update mode, character inventory system
- **Day 7 — Water tracker:** JSON-backed tracker with add/view/goal/clear modes, first real use of `try`/`except` for input validation
